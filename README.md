# regex-to-automata-visualizer
This visualizer simulates the step-by-step conversion of Regular Expressions into NFA, DFA, and Minimized DFA, Features an interactive string tester with live playback controls, allowing users to animate and trace the machine's state transitions
